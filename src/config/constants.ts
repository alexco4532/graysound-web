export const SLICK_AUTOPLAY_SPEED: number = parseInt(
  process.env.NEXT_PUBLIC_SLICK_AUTOPLAY_SPEED || '6000',
  10
);

export const SLICK_AUTOPLAY_SPEED_REVIEWS: number = parseInt(
  process.env.NEXT_PUBLIC_SLICK_AUTOPLAY_SPEED_REVIEWS || '3000',
  10
);
export const SLICK_SPEED: number = parseInt(
  process.env.NEXT_PUBLIC_SLICK_SPEED || '600',
  10
);
export const MOBILE_WIDE_WIDTH: number = parseInt(
  process.env.NEXT_PUBLIC_MOBILE_WIDE_WIDTH || '620',
  10
);
export const MOBILE_MAX_WIDTH: number = parseInt(
  process.env.NEXT_PUBLIC_MOBILE_MAX_WIDTH || '760',
  10
);
export const TABLET_MAX_WIDTH: number = parseInt(
  process.env.NEXT_PUBLIC_TABLET_MAX_WIDTH || '1024',
  10
);
